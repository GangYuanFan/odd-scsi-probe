# Code Review — 2026-08-08（v1.2.1 基準）

**審查方式**：Agency Agents 雙專家平行審查（#R1 embedded-firmware-engineer 規格級 CDB 審核 + #R2 code-reviewer 程式碼品質審查），PM 以 Linux kernel 原始碼交叉驗證關鍵發現。
**基準**：134 + 26 項測試全 PASS（但測試與實現同源同錯，見 T1）。

---

## 🔴 P0 — 立即修復（6 項）

| # | 發現 | 位置 | 影響 |
|---|------|------|------|
| **P0-1** | **READ CD (0xBE) block-type 矩陣 CDB 錯位 + 規格表誤植**：user data 旗標放進 byte 6（實為 Transfer Length MSB，codes≥8 時 TL=1,048,577）；byte 9（Main Channel Selection）恆 0 → 不回資料。且 CD_BLOCK_TYPES 引用 MMC-6 **Table 671**（Write Parameters 模式頁），READ CD 的 Expected Sector Type（Table 352）只有 0-5 合法，8-13 是 Reserved → **任何機器都會得到同一張假矩陣**，mandatory 列 8/10/13 恆 ❌ | `CD_BLOCK_TYPES` L39-54、`_read_cd_cdb()` L745-750、`probe_device()` step 5 | 旗艦功能（鑑別燒錄機 vs 唯讀機）輸出恆定假象 |
| **P0-2** | **RSOC (0xA3) allocation length 錯位**：MAINTENANCE IN 的 alloc 在 bytes 6-7，現行放 bytes 8-9 → alloc=0 → `rsoc_opcodes` 恆空 | CMDS 0xA3 條目 | Drive-Reported Opcodes 功能失效 |
| **P0-3** | **REPORT LUNS (0xA0) 只有 6-byte CDB**（規格 12-byte，alloc 在 bytes 6-7）→ 畸形指令、結果不可靠 | CMDS 0xA0 條目 | 探測結果不可信 |
| **P0-4** | **Linux resid 忽略 → GET CONFIGURATION 零填充污染**：`data = raw[:alloc]` 未用 `hdr.resid` 截斷；parser 又無視 bytes 0-1 Data Length → 真實裝置回 44B + 65491 零填充 → **99 筆假 0x0000 feature** | `scsi_execute()` L337、`parse_get_configuration()` L533-552 | 每個真實 Linux 裝置報告都帶髒資料（GUI/人讀/HTML 全受影響） |
| **P0-5** | **GUI --dangerous 確認對話框文案過期**：L14-15/L281-282 仍寫「BLANK / CLOSE TRACK/SESSION 永不執行」「不會寫入任何資料」，與引擎實際行為（真實抹碟/關 session/寫入）矛盾 → 安全誤導 | `odd_probe_gui.py` L14-15、L281-282 | 使用者可能真的抹掉測試片 |
| **P0-6** | **READ CAPACITY block_len 未 clamp**：空片 firmware 回 0xFFFFFFFF → READ 10/WRITE alloc = 4GB → `create_string_buffer` 拋 MemoryError（非 OSError，`main()` 接不住）→ traceback | `probe_device()` L704-705、L776-779 | 異常 firmware 直接 crash |

## 🟠 P1 — 近期修復（9 項）

| # | 發現 | 位置 |
|---|------|------|
| **P1-1** | **GET PERFORMANCE (0xAC) 16-byte vs 規格 12-byte**（MMC-6 Table 290：Max Descriptors 在 bytes 8-9、Type 在 byte 10）；bytes 12-15 多餘（0x20 疑似誤填） | CMDS 0xAC 條目 |
| **P1-2** | **START STOP UNIT (0x1B) 實際是 STOP**：byte1=0x01 = 僅 IMMED、START=0 → safe 模式就把馬達 spin-down；byte4=0x01 為 reserved 非零 | CMDS 0x1B 條目 |
| **P1-3** | **PLAY AUDIO 10 (0x45) TL=256**（bytes 7-8 = 0x0100；kernel `cdrom_play_blk` 實錘 TL 在 bytes 7-8）→ --dangerous 會播放 256 frames | CMDS 0x45 條目 |
| **P1-4** | **WRITE BUFFER (0x3B) paramlen=0 卻送 8 bytes**（mode 0 的 param list = 4B header + 4B data；CDB bytes 6-8 應 = 0x000008）→ 嚴格驅動回 0x24/0x26 | CMDS 0x3B 條目 + out_data 組裝 |
| **P1-5** | **READ MEDIA SERIAL (0xAB) alloc = 0x8000（32768）**（bytes 9-10 應 = 0x0080）→ 資料截斷（未解析故無害） | CMDS 0xAB 條目 |
| **P1-6** | **sense 分類不支援 descriptor format (0x72/0x73)**：key 在 byte 1、ASC/ASCQ 在 descriptor 內；現行讀 byte 2/12/13 → 誤判（實測 NOT READY/0x3A 被誤判 OTHER 而非 NEEDS_MEDIA） | `classify()` L469-471 |
| **P1-7** | **CHECK CONDITION 但 sense 空/無效時未補發 REQUEST SENSE (0x03)**：USB bridge 未實作/吞掉 auto-sense 時放棄全部分類；0x03 目前只是矩陣內 probe 非救援機制 | `classify()` 空 sense 分支 + `probe_device()` |
| **P1-8** | **Windows SCSI_PASS_THROUGH 65535 alloc 超 64KB**（GC = 76+65535 = 65611B）：MS 文件明示 buffered IOCTL 通常 <16K，應縮 alloc 至 4096（MMC 允許截斷）或改 DIRECT | `odd_probe.py` L406/419 + GC/DI/TI/MODE SENSE 10 |
| **P1-9** | **scsi_execute 內 OSError 未捕捉**：探測中途熱拔 USB ODD → 整份結果丟棄（CLI）；應 catch 回 OTHER 保留其餘 78 項 | `scsi_execute()` L326 |

## 🟡 P2 — backlog（可選）

- **P2-1** READ CD MSF (0xB9)：MSF 0:0:0 落在 lead-in（真機可能回 MEDIUM ERROR）；alloc 2352 vs 實際 2048（resid 304）→ alloc 改 2048
- **P2-2** SYNCHRONIZE CACHE (0x35) 註解「range 0 = no-op」錯誤：SBC-3 len=0 = 同步全部 cached blocks
- **P2-3** OPT-6：READ CD 10 次 probe 依 CD Read feature 存在與否先行過濾（省 10 ioctl，DEBT 既有路徑）
- **P2-4** OPT-7：連續 ≥3 TIMEOUT 後 adaptive timeout 降為 1-2s（最壞 395s → ~1-2 分鐘）
- **P2-5** OPT-8：Windows handle 貫穿 probe（少 78 次 CreateFileW）；Linux O_RDWR 失敗 fallback O_RDONLY
- **P2-6** media-family 標註 + sub-parameter 矩陣（如 READ DVD STRUCTURE format code 矩陣）→ 每種光碟片類型相容性檢測深化（產品價值，規劃中）
- **NIT**：format_human summary KeyError（`.get(k,0)`）；report_html TOOL_VERSION 停滯 v1.2.0；cache key 改 (op, SA)（0xA3 重複）；GUI 進度條前段 14 ioctl 無事件；CLI list 對 `\\.\Scsi*` 過濾

## ✅ 確認正確（#R1 逐字核對）

- 55+ CDB 正確：0x00/0x03/0x12(含 EVPD 0x80)/0x1A/0x1E/0x25/0x28/0x2B/0x2F/0x4C/0x5A/0x1C/0x1D/0x23/0x43/0x46/0x4A/0x4B/0x51/0x52/0xA2/0xA4/0xB5/0xAD/0xA8/0xAA/0x2A/0x2E/0xB6/0xBD/0xBB/0x5C/0x5D/0xA1/0xA6/0x5B/0x56/0x58/0x54/0xA5/0x42/0xA3-SENDKEY/0xBF 等
- SG_IO `sg_io_hdr` ctypes 佈局、Windows `SCSI_PASS_THROUGH`（offset/sizeof 76 與 ntddscsi.h 相符）、sense 固定格式 ASC/ASCQ 解析、`parse_inquiry`/`parse_get_configuration`/`parse_disc_info`（Disc Type = byte 8 bits 3-0）/`parse_rsoc`、direction 對應、safe/dangerous 分流、0x12 快取失敗保護
- **待真機驗證**：B1 修法後各 block size 回傳、R6 lead-in、P1-3 播放行為、0x42/0xA3-SENDKEY/0xBF 最終章節、Windows 實體 ODD INQUIRY 成功路徑

## 📋 測試覆蓋缺口（134 項全 PASS 的盲點）

1. **無 byte-exact CDB 黃金向量測試**——P0-1/2/3 漏網主因（測試固化了錯誤 CDB：`_read_cd_cdb(8)[6]==0x10`、0xA3 alloc）
2. resid/零填充截斷（P0-4 漏網主因）
3. descriptor-format sense 0x72（P1-6 漏網主因）
4. block_len 0xFFFFFFFF clamp（P0-6）
5. probe_device 中途 OSError 結果保留
6. Windows scsi_execute 純邏輯（DataIn/total/out_data）抽平台無關測試
7. report_html.py 完全無測試（XSS escape + 冒煙）
8. JSON round-trip

## 🎯 老闆三大關注點對應

1. **每種光碟片類型的 command 相容性檢測** → ⚠️ P0-1 直接打中：現行 READ CD block type 矩陣（鑑別燒錄機/唯讀機的旗艦功能）CDB 送錯，任何機器輸出同一張假矩陣。修復後建議深化（P2-6）：media-family 標註 + READ DVD STRUCTURE format / GET PERFORMANCE type 等 sub-parameter 矩陣
2. **所有 ODD SCSI command 的 CDB 送/收正確** → 3 🔴（P0-1/2/3）+ 5 🟠 CDB 錯誤（P1-1~5）待修；送端（SG_DXFER_TO_DEV/SCSI_IOCTL_DATA_OUT）與收端（resid 截斷）需一併修正
3. **Request Sense 檢查錯誤能力** → 具備但兩缺口：P1-6 descriptor format 誤判、P1-7 auto-sense 空時無 REQUEST SENSE 救援機制；sense buffer 32B 足夠（fixed 18B / descriptor 首 10B）

---

## ✅ 修復狀態（2026-08-08 v1.3.0 已交付）

**驗證**：QA 獨立複核 15/15 PASS（195 + 26 測試全綠）、PM 以 kernel 原始碼再驗證。
**19 個微提交**（P0×6 + P1×9 + 版本/GUI 同步），commit hygiene 乾淨、檔案隔離無越線。

**重要更正**：原 P1-2（START STOP UNIT 0x1B「實際是 STOP」）為 **#R1 誤報**——SBC-3/SFF-8020i 的 START bit 在 **byte 4 bit 0**（kernel `sd_start_stop_device` `cmd[4] |= 1` 實錘），原版 CDB `[0x1B, 0x01, 0, 0, 0x01, 0]` 本就正確；RD 實作時查證後保留原值並修正註解。

**新增 P2 backlog**（QA 觀察，非本次引入）：
- LOG SENSE (0x4C) CDB alloc 0xFFFF vs buffer 4096 不一致（與 P1-8 同類，建議改 0x1000）
- tests/test_odd_gui_logic.py + gui_smoke.py 的 FAKE_RESULT 仍為舊 block_type_matrix schema（code 0/8/13），fixture 過期需同步
- format_human summary 直接索引（`.get` 防禦，低風險）
